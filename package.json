{
  "name": "microservice-ping-test",
  "version": "0.0.1",
  "description": "Microservice ping test",
  "main": "index.js",
  "scripts": {
    "build": "lab -L -t 95 -r lcov -o ./test/artifacts/coverage.lcov",
    "test": "lab",
    "test-verbose": "lab -v",
    "lint": "lab -L",
    "cover": "lab -c",
    "all-the-things": "lab -vLc",
    "all-the-things-html": "lab -cLv -r html -o ./test/artifacts/coverage.html; open ./test/artifacts/coverage.html"
  },
  "author": "Rocky Assad <rocky.assad@premisehealth.com>",
  "license": "ISC",
  "devDependencies": {
    "code": "^2.1.0",
    "eslint": "^2.1.0",
    "lab": "^8.2.0"
  },
  "dependencies": {
    "config": "^1.19.0",
    "service-adapter": "0.0.4"
  }
}
